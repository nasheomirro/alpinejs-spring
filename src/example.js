// spring values need to be accessible as if from x-data itself:
// - access through x-init or init() --> not possible
// - access through the scope
// spring values should be settable with the options { hard } or { soft }
// spring tasks need to be destroyed after by removing them from raf
